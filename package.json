{
  "name": "noquelle-site",
  "version": "1.0.0",
  "private": true,
  "description": "Лендинг Noquelle: статика + функции Netlify (публикация каталога и учёт остатков)",
  "dependencies": {
    "@netlify/blobs": "^10.7.11",
    "@netlify/functions": "^5.3.0"
  }
}
